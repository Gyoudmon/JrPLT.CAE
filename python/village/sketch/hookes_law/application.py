# 场景：胡克定律

# 定义函数：
def l(m, 原始长度, 限度):
    if m <= 限度:
        return 0.1614 * m + 原始长度
    else:
        print(f"啊啊啊啊啊啊，弹簧坏掉啦！")
        return 0

# 调用函数：
print(l(45.0, 50.0, 100.0))
print(l(120.0, 50.0, 100.0))
